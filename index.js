/*
    Primary file for the API
*/

// Dependencies

// Declare the app
var app = {};

// Initialize function
app.init = function() {
    // Start the server
};

// Execute the app
app.init();

// Export the app
module.exports = app;
